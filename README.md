# Hospital Management System Database Design

## 📌 Project Overview
This project involves the design and implementation of a relational database for a medium-sized hospital management system. The system handles patient records, doctor schedules, appointments, examinations, and medication prescriptions.

## 🛠 Tech Stack
* **Modeling:** Draw.io (Conceptual & Logical ERD)
* **Database Engine:** PostgreSQL
* **Language:** SQL (DDL & DML)

## 📂 Project Structure
* **`conceptual_diagram.png`**: The high-level Entity-Relationship Diagram (ERD).
* **`logical_diagram.png`**: The detailed schema showing tables, PK/FK constraints, and data types.
* **`schema.sql`**: The full executable SQL script containing `CREATE TABLE`, `INDEX`, and sample `INSERT` statements.

## 🚀 How to Run
1. Download the `schema.sql` file.
2. Open any PostgreSQL interface (DataGrip, pgAdmin, or db-fiddle.com).
3. Run the script to generate the tables and populate them with sample data.
